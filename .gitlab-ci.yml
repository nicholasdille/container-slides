stages:
- check
- build

lint:
  stage: check
  image: golang:1.25.4
  script:
  - go fmt .

audit:
  stage: check
  image: golang:1.25.4
  script:
  - go vet .

build:
  stage: build
  image: golang:1.25.4
  variables:
    version: $CI_COMMIT_REF_NAME
  script:
  - |
    go build \
        -ldflags "-X main.Version=${version} -X 'main.Author=${AUTHOR}'" \
        -o hello \
        .
  - ./hello

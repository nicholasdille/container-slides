# Rollout of Visual Studio Code

## Prerequisites

Make sure your have rolled out the virtual machines in `../000_rollout` and GitLab in `../001_server`.

## Deploy Visual Studio Code

```shell
bash bootstrap.sh
```

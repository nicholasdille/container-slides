spec:
  inputs:
    context:
      type: string
      default: .
    image:
      type: string
---
exec:
  command:
  - docker
  - build
  - ${{ inputs.context }}
  - --provenance=false
  - --tag=${{ inputs.image }}
